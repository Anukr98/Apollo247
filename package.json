{
  "name": "apollo-hospitals",
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "bootstrap:web": "lerna bootstrap --ignore mobile*",
    "bootstrap:mobile": "lerna bootstrap --ignore web*"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.15.0",
    "typescript": "^3.5.2"
  }
}
