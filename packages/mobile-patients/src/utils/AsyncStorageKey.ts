// Constant file to keep AsyncStorage Keys

export const SKIP_LOCATION_PROMPT = 'SKIP_LOCATION_PROMPT';

/**
 * since we already navigating user to signup screen from splash screen if in case user close his app from their,
 * so we are using this async key to store patient profile and fill details on the signup page.
 */
export const LOGIN_PROFILE = 'LOGIN_PROFILE';

export const HEALTH_CREDITS = 'HEALTH_CREDITS';
