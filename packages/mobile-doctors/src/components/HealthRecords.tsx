import React from 'react';
import { View } from 'react-native';

export interface HealthRecordsProps {}

export const HealthRecords: React.FC<HealthRecordsProps> = (props) => {
  return <View style={{ flex: 1, backgroundColor: 'white' }} />;
};
